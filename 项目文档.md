# 卡牌游戏项目文档

## 1. 项目概述

这是一个基于Cocos2d-x引擎开发的卡牌游戏，采用MVC架构设计，实现了卡牌匹配、替换、撤销等核心功能。

## 2. 项目结构

```
Classes/
├── configs/          # 配置文件
│   └── models/       # 配置模型
│       ├── CardConfig.h/cpp      # 卡牌配置
│       └── LevelConfig.h/cpp     # 关卡配置
├── controllers/      # 控制器
│   ├── CardController.h/cpp      # 卡牌控制器
│   └── GameController.h/cpp      # 游戏主控制器
├── managers/         # 管理器
│   └── UndoManager.h/cpp         # 撤销管理器
├── models/           # 数据模型
│   ├── CardModel.h/cpp           # 卡牌模型
│   ├── GameModel.h/cpp           # 游戏模型
│   └── UndoModel.h/cpp           # 撤销模型
├── services/         # 服务层
│   ├── CardService.h/cpp         # 卡牌服务
│   └── CommandService.h/cpp      # 命令服务
├── views/            # 视图层
│   ├── CardView.h/cpp            # 卡牌视图
│   └── GameView.h/cpp            # 游戏视图
├── AppDelegate.h/cpp             # 应用入口
└── TestScene.h/cpp               # 测试场景
```

## 3. 核心功能

1. **卡牌匹配**：玩家可以使用手牌与桌面卡牌进行匹配
2. **手牌替换**：玩家可以替换当前的手牌
3. **撤销功能**：支持撤销上一步操作
4. **关卡配置**：从JSON文件加载关卡配置
5. **动态卡牌生成**：根据配置动态生成卡牌
6. **动画效果**：卡牌移动、匹配、替换等动画效果
7. **中文显示**：支持中文文本显示

## 4. 技术架构

### 4.1 MVC架构

- **Model**：负责数据管理和业务逻辑
- **View**：负责界面显示和用户交互
- **Controller**：负责协调Model和View，处理用户输入

### 4.2 命令模式

使用命令模式实现撤销/重做功能：
- 每个操作封装为一个命令对象
- 命令执行后保存到命令历史记录
- 撤销时执行命令的撤销操作
- 重做时重新执行命令

### 4.3 JSON配置

- 使用JSON文件存储关卡配置
- 支持动态加载不同关卡
- 配置包括卡牌位置、类型、花色等信息

## 5. 主要类说明

### 5.1 GameController

游戏主控制器，负责协调游戏各组件：
- 初始化游戏
- 处理用户输入
- 管理游戏状态
- 协调模型和视图

### 5.2 GameModel

游戏数据模型，管理游戏数据：
- 卡牌数据
- 游戏状态
- 手牌顺序
- 顶部手牌

### 5.3 GameView

游戏视图，负责游戏界面显示：
- 卡牌显示
- 动画效果
- UI元素
- 用户交互

### 5.4 CardModel

卡牌数据模型，管理单张卡牌数据：
- 卡牌属性
- 位置信息
- 状态信息

### 5.5 CardView

卡牌视图，负责单张卡牌显示：
- 卡牌图像
- 点击效果
- 移动动画

### 5.6 LevelConfig

关卡配置，从JSON文件加载关卡数据：
- 桌面卡牌配置
- 手牌配置
- 关卡布局

### 5.7 UndoManager

撤销管理器，管理撤销/重做功能：
- 命令历史记录
- 撤销操作
- 重做操作

### 5.8 CommandService

命令服务，执行游戏命令：
- 匹配卡牌命令
- 替换手牌命令
- 命令执行和撤销

## 6. 游戏流程

1. **游戏初始化**：AppDelegate启动，创建GameController
2. **加载关卡**：GameController从JSON文件加载关卡配置
3. **生成游戏模型**：根据关卡配置生成GameModel
4. **创建游戏视图**：GameController创建GameView，显示游戏界面
5. **用户交互**：用户点击卡牌，触发游戏逻辑
6. **处理游戏逻辑**：GameController处理用户输入，更新GameModel
7. **更新视图**：GameView根据GameModel更新界面
8. **动画效果**：播放卡牌移动、匹配、替换等动画
9. **撤销/重做**：用户点击撤销/重做按钮，执行相应操作

## 7. 中文显示解决方案

1. **使用系统字体**：`Label::createWithSystemFont("回退", "Microsoft YaHei", 24)`
2. **设置执行字符集**：`#pragma execution_character_set("utf-8")`
3. **正确的文件编码**：所有包含中文的文件使用UTF-8编码

## 8. 开发环境

- **引擎**：Cocos2d-x
- **语言**：C++
- **开发工具**：Visual Studio
- **平台**：Windows（支持跨平台开发）

## 9. 编译运行

1. 打开`proj.win32/card.sln`
2. 选择Debug或Release配置
3. 编译项目
4. 运行生成的可执行文件

## 10. 扩展开发

### 10.1 添加新关卡

1. 在`Resources`目录下创建新的JSON配置文件
2. 按照现有格式编写关卡配置
3. 修改GameController中的关卡加载逻辑

### 10.2 添加新卡牌类型

1. 在CardConfig中添加新的卡牌类型枚举
2. 更新卡牌生成逻辑
3. 添加相应的卡牌资源

### 10.3 添加新功能

1. 在GameModel中添加新的数据字段
2. 在GameController中添加新的逻辑处理
3. 在GameView中添加新的UI元素和动画

## 11. 代码规范

1. **命名规范**：使用驼峰命名法
2. **注释规范**：为所有类、函数和重要变量添加注释
3. **文件结构**：按照MVC架构组织文件
4. **代码风格**：统一的代码缩进和格式

## 12. 注意事项

1. 确保所有资源文件路径正确
2. 注意内存管理，避免内存泄漏
3. 测试不同屏幕分辨率下的显示效果
4. 注意性能优化，避免过多的资源加载和计算

## 13. 函数框架图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                               AppDelegate                                │
└─────────────────────────────────────┬─────────────────────────────────────┘
                                      │
┌─────────────────────────────────────▼─────────────────────────────────────┐
│                               GameController                              │
├────────────────┬────────────────┬────────────────┬────────────────┬───────┤
│   GameModel   │    GameView    │ UndoManager    │ CommandService │ ...   │
└────────────────┴────────────────┴────────────────┴────────────────┴───────┘
        │                │                │                │
        ▼                ▼                ▼                ▼
┌────────────────┐┌────────────────┐┌────────────────┐┌────────────────┐
│   CardModel    ││    CardView    ││    UndoModel    ││ ReplaceCommand │
└────────────────┘└────────────────┘└────────────────┘└────────────────┘
        │                │
        ▼                ▼
┌────────────────┐┌────────────────┐
│  CardConfig    ││  LevelConfig   │
└────────────────┘└────────────────┘
```

## 14. 主要函数说明

### 14.1 GameController

| 函数名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `startGame` | 启动游戏 | levelId: 关卡ID | void |
| `handleCardClick` | 处理卡牌点击 | cardView: 被点击的卡牌视图 | bool |
| `replaceTopHandCard` | 替换顶部手牌 | clickedCardId: 被点击的卡牌ID | void |
| `matchTableCard` | 匹配桌面卡牌 | tableCardId: 桌面卡牌ID | void |
| `updateUI` | 更新UI | 无 | void |
| `handleUndoButtonClick` | 处理撤销按钮点击 | 无 | void |
| `handleRedoButtonClick` | 处理重做按钮点击 | 无 | void |

### 14.2 GameModel

| 函数名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `addCard` | 添加卡牌 | cardModel: 卡牌模型 | void |
| `removeCard` | 移除卡牌 | cardId: 卡牌ID | void |
| `getCardById` | 根据ID获取卡牌 | cardId: 卡牌ID | CardModel* |
| `getHandCards` | 获取手牌列表 | 无 | vector<CardModel*> |
| `getTableCards` | 获取桌面卡牌列表 | 无 | vector<CardModel*> |
| `setTopHandCardId` | 设置顶部手牌ID | cardId: 卡牌ID | void |
| `getTopHandCard` | 获取顶部手牌 | 无 | CardModel* |
| `reset` | 重置游戏模型 | 无 | void |

### 14.3 GameView

| 函数名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `updateGameView` | 更新游戏视图 | gameModel: 游戏模型 | void |
| `addCardView` | 添加卡牌视图 | cardView: 卡牌视图 | void |
| `removeCardView` | 移除卡牌视图 | cardId: 卡牌ID | void |
| `getCardView` | 获取卡牌视图 | cardId: 卡牌ID | CardView* |
| `updateCardView` | 更新卡牌视图 | cardModel: 卡牌模型 | void |
| `playMatchAnimation` | 播放匹配动画 | topCardId: 顶部卡牌ID, tableCardId: 桌面卡牌ID | void |
| `playReplaceAnimation` | 播放替换动画 | clickedCardId: 被点击的卡牌ID, topCardId: 顶部卡牌ID | void |
| `playUndoAnimation` | 播放撤销动画 | cardId: 卡牌ID, originalPos: 原始位置 | void |

### 14.4 LevelConfig

| 函数名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `loadLevelConfig` | 加载关卡配置 | levelConfigPath: 配置文件路径 | bool |
| `getPlayfieldCards` | 获取桌面卡牌配置 | 无 | const vector<CardData>& |
| `getStackCards` | 获取堆叠卡牌配置 | 无 | const vector<CardData>& |

### 14.5 CardConfig

| 函数名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `getTextureName` | 获取卡牌纹理名称 | suit: 花色, rank: 点数 | string |
| `getSuitTexturePath` | 获取花色纹理路径 | suit: 花色 | string |
| `getBigNumberTexturePath` | 获取大数字纹理路径 | suit: 花色, rank: 点数 | string |
| `getSmallNumberTexturePath` | 获取小数字纹理路径 | suit: 花色, rank: 点数 | string |
| `createTestData` | 创建测试数据 | 无 | vector<CardData> |

## 15. 后续改进方向

1. **添加音效**：为游戏添加背景音乐和音效
2. **优化动画**：添加更多精美的动画效果
3. **添加关卡选择**：实现关卡选择功能
4. **添加计分系统**：实现游戏计分和排行榜
5. **优化性能**：优化游戏性能，支持更多卡牌和更复杂的关卡
6. **添加网络功能**：实现联网对战功能
7. **支持更多平台**：支持Android、iOS等平台
