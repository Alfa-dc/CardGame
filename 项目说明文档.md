# 卡牌游戏项目完整说明文档

## 目录
1. [项目概述](#项目概述)
2. [项目架构](#项目架构)
3. [MVC架构详解](#mvc架构详解)
4. [核心模块说明](#核心模块说明)
5. [Cocos2d-x核心概念](#cocos2d-x核心概念)
6. [项目资源说明](#项目资源说明)
7. [游戏流程详解](#游戏流程详解)
8. [关键设计模式](#关键设计模式)
9. [常用函数说明](#常用函数说明)

---

## 项目概述

这是一个基于Cocos2d-x引擎开发的卡牌匹配游戏，玩家需要通过点击手牌和桌面牌来完成匹配任务。

### 游戏规则
- **手牌（紫色区域）**：玩家可以点击手牌来切换顶手牌
- **桌面牌（橙色区域）**：显示在桌面上的卡牌
- **匹配规则**：顶手牌与桌面牌的花色和点数相同时可以匹配
- **撤销/重做**：支持Z键撤销，Y键重做

### 项目特点
- 采用MVC架构，代码结构清晰
- 支持撤销/重做功能
- 完整的动画效果
- 模块化设计，易于扩展

---

## 项目架构

### 目录结构
```
card/
├── Classes/                    # 游戏代码目录
│   ├── configs/               # 配置模块
│   │   ├── loaders/          # 配置加载器
│   │   │   └── LevelConfigLoader.h/cpp
│   │   └── models/           # 配置数据模型
│   │       ├── CardConfig.h
│   │       └── LevelConfig.h
│   ├── controllers/          # 控制器层
│   │   ├── GameController.h/cpp
│   │   ├── CardController.h/cpp
│   │   ├── PlayFieldController.h/cpp
│   │   └── StackController.h/cpp
│   ├── models/               # 数据模型层
│   │   ├── GameModel.h/cpp
│   │   ├── CardModel.h/cpp
│   │   └── UndoModel.h/cpp
│   ├── views/                # 视图层
│   │   ├── GameView.h/cpp
│   │   └── CardView.h/cpp
│   ├── services/             # 服务层
│   │   ├── CommandService.h/cpp
│   │   └── CardService.h/cpp
│   ├── managers/             # 管理器层
│   │   ├── UndoManager.h/cpp
│   │   └── LevelManager.h/cpp
│   ├── generators/           # 生成器层
│   │   └── GameModelFromLevelGenerator.h/cpp
│   ├── AppDelegate.h/cpp     # 应用程序入口
│   └── CardConfig.cpp        # 卡牌配置
├── Resources/                # 资源目录
│   ├── fonts/               # 字体文件
│   └── res/                 # 游戏资源
│       ├── number/          # 数字图片
│       ├── suits/           # 花色图片
│       └── card_general.png # 卡牌背景
├── cocos2d/                 # Cocos2d-x引擎
└── build/                   # 构建目录
```

### 核心模块关系图
```
AppDelegate (应用入口)
    ↓
GameController (游戏主控制器)
    ├─→ GameModel (游戏数据模型)
    ├─→ GameView (游戏视图)
    ├─→ CommandService (命令服务)
    ├─→ UndoManager (撤销管理器)
    ├─→ CardController (卡牌控制器)
    ├─→ PlayFieldController (桌面控制器)
    └─→ StackController (手牌堆叠控制器)
```

---

## MVC架构详解

### 什么是MVC架构？
MVC（Model-View-Controller）是一种软件设计模式，将应用程序分为三个核心部分：
- **Model（模型）**：负责数据管理和业务逻辑
- **View（视图）**：负责界面显示和用户交互
- **Controller（控制器）**：负责协调模型和视图，处理用户输入

### 本项目的MVC实现

#### Model层（数据模型层）
**职责**：
- 存储游戏数据
- 管理游戏状态
- 提供数据访问接口

**核心类**：
- [GameModel](file:///d:/Code/cocos_work/card/Classes/models/GameModel.h)：游戏数据模型
- [CardModel](file:///d:/Code/cocos_work/card/Classes/models/CardModel.h)：卡牌数据模型
- [UndoModel](file:///d:/Code/cocos_work/card/Classes/models/UndoModel.h)：撤销/重做模型

**示例代码**：
```cpp
// GameModel管理所有卡牌
GameModel* gameModel = new GameModel();
CardModel* card = CardModel::createFromConfig(configData);
gameModel->addCard(card);

// 获取顶手牌
CardModel* topCard = gameModel->getTopHandCard();
```

#### View层（视图层）
**职责**：
- 显示游戏界面
- 处理用户交互
- 播放动画效果

**核心类**：
- [GameView](file:///d:/Code/cocos_work/card/Classes/views/GameView.h)：游戏主视图
- [CardView](file:///d:/Code/cocos_work/card/Classes/views/CardView.h)：卡牌视图

**示例代码**：
```cpp
// 创建游戏视图
GameView* gameView = GameView::create();
gameView->setCardClickCallback([this](CardView* view) {
    this->handleCardClick(view);
});

// 添加卡牌视图
CardView* cardView = CardView::createWithModel(cardModel);
gameView->addCardView(cardView);
```

#### Controller层（控制器层）
**职责**：
- 协调模型和视图
- 处理用户输入
- 实现游戏逻辑

**核心类**：
- [GameController](file:///d:/Code/cocos_work/card/Classes/controllers/GameController.h)：游戏主控制器
- [CardController](file:///d:/Code/cocos_work/card/Classes/controllers/CardController.h)：卡牌控制器
- [PlayFieldController](file:///d:/Code/cocos_work/card/Classes/controllers/PlayFieldController.h)：桌面控制器
- [StackController](file:///d:/Code/cocos_work/card/Classes/controllers/StackController.h)：手牌堆叠控制器

**示例代码**：
```cpp
// 处理卡牌点击
bool GameController::handleCardClick(CardView* cardView) {
    int cardId = cardView->getCardId();
    CardModel* card = _gameModel->getCardById(cardId);
    
    if (card->getType() == CardType::HAND_CARD) {
        replaceTopHandCard(cardId);
    } else {
        matchTableCard(cardId);
    }
    
    return true;
}
```

### MVC数据流向
```
用户操作 → View捕获事件 → Controller处理 → 更新Model → Model通知View更新 → View刷新显示
```

---

## 核心模块说明

### 1. AppDelegate（应用程序入口）

**文件位置**：[AppDelegate.h](file:///d:/Code/cocos_work/card/Classes/AppDelegate.h)

**职责**：
- 初始化Cocos2d-x引擎
- 创建游戏窗口
- 管理应用程序生命周期
- 启动游戏主控制器

**关键方法**：
```cpp
// 初始化OpenGL上下文属性
virtual void initGLContextAttrs();

// 应用程序启动完成时调用
virtual bool applicationDidFinishLaunching();

// 应用程序进入后台时调用
virtual void applicationDidEnterBackground();

// 应用程序重新进入前台时调用
virtual void applicationWillEnterForeground();
```

**Cocos2d-x核心概念**：
- **Director（导演）**：Cocos2d-x的核心管理器，负责场景管理和渲染
- **Scene（场景）**：游戏中的一个完整界面，如主菜单、游戏界面
- **OpenGL上下文**：用于渲染图形的底层图形API

### 2. GameController（游戏主控制器）

**文件位置**：[GameController.h](file:///d:/Code/cocos_work/card/Classes/controllers/GameController.h)

**职责**：
- 协调所有游戏组件
- 管理游戏流程
- 处理用户交互
- 实现游戏核心逻辑

**关键方法**：
```cpp
// 初始化游戏控制器
bool init();

// 开始游戏
void startGame(int levelId);

// 处理卡牌点击
bool handleCardClick(CardView* cardView);

// 处理撤销/重做
void handleUndoButtonClick();
void handleRedoButtonClick();
void handleResetButtonClick();
```

**游戏流程**：
```
1. 加载关卡配置
2. 生成游戏模型
3. 初始化控制器
4. 初始化游戏视图
5. 设置事件监听器
6. 创建所有卡牌视图
7. 更新UI
8. 播放入场动画
9. 运行场景
```

### 3. GameModel（游戏数据模型）

**文件位置**：[GameModel.h](file:///d:/Code/cocos_work/card/Classes/models/GameModel.h)

**职责**：
- 存储所有卡牌数据
- 管理手牌和桌面牌
- 跟踪顶手牌状态
- 支持序列化和反序列化

**关键数据结构**：
```cpp
// 所有卡牌映射表
std::unordered_map<int, CardModel*> _cards;

// 当前顶手牌ID
int _topHandCardId;

// 手牌顺序列表
std::vector<int> _handCardOrder;
```

**关键方法**：
```cpp
// 添加卡牌
void addCard(CardModel* cardModel);

// 移除卡牌
void removeCard(int cardId);

// 获取顶手牌
CardModel* getTopHandCard() const;

// 序列化游戏状态
cocos2d::ValueMap serialize() const;

// 反序列化游戏状态
bool deserialize(const cocos2d::ValueMap& data);
```

### 4. CardModel（卡牌数据模型）

**文件位置**：[CardModel.h](file:///d:/Code/cocos_work/card/Classes/models/CardModel.h)

**职责**：
- 存储单张卡牌的数据
- 支持序列化和反序列化

**卡牌属性**：
```cpp
int _id;                          // 卡牌ID
CardConfig::Suit _suit;           // 花色（红心、方块、梅花、黑桃）
int _rank;                        // 点数（1-13，1=A, 11=J, 12=Q, 13=K）
CardConfig::CardType _type;       // 类型（手牌或桌面牌）
cocos2d::Vec2 _position;         // 当前位置
cocos2d::Vec2 _originalPosition; // 原始位置
std::string _textureName;        // 纹理名称
bool _isTopCard;                  // 是否为顶手牌
bool _isVisible;                  // 是否可见
int _zOrder;                      // Z轴值（显示层级）
```

**花色枚举**：
```cpp
enum class Suit {
    HEART = 0,   // 红心
    DIAMOND = 1, // 方块
    CLUB = 2,    // 梅花
    SPADE = 3    // 黑桃
};
```

**卡牌类型枚举**：
```cpp
enum class CardType {
    HAND_CARD = 0,  // 手牌
    TABLE_CARD = 1  // 桌面牌
};
```

### 5. GameView（游戏主视图）

**文件位置**：[GameView.h](file:///d:/Code/cocos_work/card/Classes/views/GameView.h)

**职责**：
- 显示游戏场景
- 管理所有卡牌视图
- 显示UI按钮
- 播放动画效果

**关键方法**：
```cpp
// 更新游戏视图
void updateGameView(const GameModel* gameModel);

// 添加卡牌视图
void addCardView(CardView* cardView);

// 移除卡牌视图
void removeCardView(int cardId);

// 播放匹配动画
void playMatchAnimation(int topCardId, int tableCardId);

// 播放替换动画
void playReplaceAnimation(int clickedCardId, int topCardId);
```

### 6. CardView（卡牌视图）

**文件位置**：[CardView.h](file:///d:/Code/cocos_work/card/Classes/views/CardView.h)

**职责**：
- 显示单张卡牌
- 处理卡牌点击事件
- 播放卡牌动画

**视觉组件**：
```cpp
cocos2d::Sprite* _background;         // 背景精灵
cocos2d::Sprite* _numberSprite;       // 左上角数字
cocos2d::Sprite* _bigNumberSprite;    // 右下角大数字
cocos2d::Sprite* _topRightSuitSprite; // 右下角花色
```

**关键方法**：
```cpp
// 使用模型创建卡牌视图
static CardView* createWithModel(const CardModel* cardModel);

// 使用模型更新卡牌视图
void updateWithModel(const CardModel* cardModel);

// 移动卡牌到指定位置
void moveToPosition(const cocos2d::Vec2& target, float duration = 0.3f);

// 设置选中状态
void setSelected(bool selected);

// 设置顶手牌状态
void setAsTopCard(bool isTop);
```

### 7. CommandService（命令服务）

**文件位置**：[CommandService.h](file:///d:/Code/cocos_work/card/Classes/services/CommandService.h)

**职责**：
- 管理游戏命令
- 实现撤销/重做功能
- 记录操作历史

**关键方法**：
```cpp
// 执行替换顶手牌命令
void executeReplaceTopCardCommand(int clickedCardId, GameModel* gameModel);

// 执行匹配桌面卡牌命令
void executeMatchTableCardCommand(int tableCardId, GameModel* gameModel);

// 撤销操作
ActionRecord* undo(GameModel* gameModel);

// 重做操作
ActionRecord* redo(GameModel* gameModel);

// 检查是否可以撤销/重做
bool canUndo() const;
bool canRedo() const;
```

### 8. UndoManager（撤销管理器）

**文件位置**：[UndoManager.h](file:///d:/Code/cocos_work/card/Classes/managers/UndoManager.h)

**职责**：
- 管理撤销/重做操作
- 提供撤销/重做接口

**关键方法**：
```cpp
// 执行撤销操作
void undo();

// 执行重做操作
void redo();

// 检查是否可以撤销/重做
bool canUndo() const;
bool canRedo() const;

// 清空历史
void clearHistory();
```

### 9. CardService（卡牌服务）

**文件位置**：[CardService.h](file:///d:/Code/cocos_work/card/Classes/services/CardService.h)

**职责**：
- 提供卡牌匹配逻辑
- 提供卡牌工具函数

**关键方法**：
```cpp
// 检查两张卡牌是否匹配
static bool areCardsMatchable(const CardModel* card1, const CardModel* card2);

// 获取卡牌纹理名称
static std::string getTextureName(CardConfig::Suit suit, int rank);
```

---

## Cocos2d-x核心概念

### 1. Director（导演）

**概念**：Director是Cocos2d-x的核心管理器，负责管理场景、渲染循环、动画等。

**常用方法**：
```cpp
// 获取Director单例
cocos2d::Director* director = cocos2d::Director::getInstance();

// 运行场景
director->runWithScene(scene);

// 替换场景
director->replaceScene(scene);

// 暂停/恢复场景
director->pause();
director->resume();

// 设置帧率
director->setAnimationInterval(1.0 / 60);  // 60 FPS
```

### 2. Scene（场景）

**概念**：Scene是游戏中的一个完整界面，如主菜单、游戏界面、设置界面等。

**创建场景**：
```cpp
// 使用CREATE_FUNC宏创建场景
GameView* gameView = GameView::create();

// 场景继承自cocos2d::Scene
class GameView : public cocos2d::Scene {
public:
    CREATE_FUNC(GameView);  // 创建场景的宏
    
    virtual bool init() override {
        // 初始化代码
        return true;
    }
};
```

### 3. Node（节点）

**概念**：Node是所有可显示对象的基础类，包括Scene、Sprite、Label等。

**常用属性**：
```cpp
// 位置
node->setPosition(cocos2d::Vec2(100, 100));

// 缩放
node->setScale(1.5f);

// 旋转
node->setRotation(45.0f);

// Z轴值（显示层级）
node->setLocalZOrder(10);

// 可见性
node->setVisible(true);

// 锚点
node->setAnchorPoint(cocos2d::Vec2(0.5, 0.5));  // 中心点
```

### 4. Sprite（精灵）

**概念**：Sprite是显示图片的节点，用于显示卡牌、背景等。

**创建精灵**：
```cpp
// 从文件创建
cocos2d::Sprite* sprite = cocos2d::Sprite::create("image.png");

// 从帧缓存创建
cocos2d::Sprite* sprite = cocos2d::Sprite::createWithSpriteFrameName("card.png");

// 设置纹理
sprite->setTexture("new_texture.png");

// 设置内容大小
sprite->setContentSize(cocos2d::Size(100, 150));
```

### 5. Action（动作）

**概念**：Action用于实现动画效果，如移动、缩放、旋转等。

**常用动作**：
```cpp
// 移动动作
auto move = cocos2d::MoveTo::create(0.3f, cocos2d::Vec2(100, 100));
sprite->runAction(move);

// 缩放动作
auto scale = cocos2d::ScaleTo::create(0.2f, 1.2f);
sprite->runAction(scale);

// 旋转动作
auto rotate = cocos2d::RotateTo::create(0.5f, 90.0f);
sprite->runAction(rotate);

// 淡入淡出
auto fadeOut = cocos2d::FadeOut::create(0.5f);
auto fadeIn = cocos2d::FadeIn::create(0.5f);

// 序列动作（按顺序执行）
auto sequence = cocos2d::Sequence::create(
    cocos2d::MoveTo::create(0.3f, cocos2d::Vec2(100, 100)),
    cocos2d::ScaleTo::create(0.2f, 1.2f),
    nullptr
);
sprite->runAction(sequence);

// 并行动作（同时执行）
auto spawn = cocos2d::Spawn::create(
    cocos2d::MoveTo::create(0.3f, cocos2d::Vec2(100, 100)),
    cocos2d::ScaleTo::create(0.3f, 1.2f),
    nullptr
);
sprite->runAction(spawn);

// 回调动作
auto callback = cocos2d::CallFunc::create([](){
    CCLOG("Action completed!");
});
auto sequence = cocos2d::Sequence::create(move, callback, nullptr);
sprite->runAction(sequence);
```

### 6. EventListener（事件监听器）

**概念**：EventListener用于监听用户输入事件，如触摸、键盘、鼠标等。

**触摸事件监听器**：
```cpp
// 创建单点触摸监听器
auto listener = cocos2d::EventListenerTouchOneByOne::create();

// 设置触摸开始回调
listener->onTouchBegan = [](cocos2d::Touch* touch, cocos2d::Event* event) {
    // 获取触摸点
    cocos2d::Vec2 pos = touch->getLocation();
    
    // 检查是否在节点范围内
    cocos2d::Node* node = event->getCurrentTarget();
    if (node->getBoundingBox().containsPoint(pos)) {
        return true;  // 接受触摸事件
    }
    return false;  // 忽略触摸事件
};

// 设置触摸结束回调
listener->onTouchEnded = [](cocos2d::Touch* touch, cocos2d::Event* event) {
    CCLOG("Touch ended");
};

// 添加监听器到事件分发器
cocos2d::Director::getInstance()->getEventDispatcher()
    ->addEventListenerWithSceneGraphPriority(listener, node);
```

**键盘事件监听器**：
```cpp
// 创建键盘监听器
auto listener = cocos2d::EventListenerKeyboard::create();

// 设置按键按下回调
listener->onKeyPressed = [](cocos2d::EventKeyboard::KeyCode keyCode, cocos2d::Event* event) {
    if (keyCode == cocos2d::EventKeyboard::KeyCode::KEY_Z) {
        CCLOG("Z key pressed - Undo");
    } else if (keyCode == cocos2d::EventKeyboard::KeyCode::KEY_Y) {
        CCLOG("Y key pressed - Redo");
    }
};

// 添加监听器
cocos2d::Director::getInstance()->getEventDispatcher()
    ->addEventListenerWithSceneGraphPriority(listener, this);
```

### 7. ValueMap（值映射）

**概念**：ValueMap是Cocos2d-x提供的键值对容器，用于存储和传递数据。

**使用示例**：
```cpp
// 创建ValueMap
cocos2d::ValueMap data;

// 添加数据
data["id"] = cocos2d::Value(1);
data["name"] = cocos2d::Value("Card");
data["positionX"] = cocos2d::Value(100.0f);
data["positionY"] = cocos2d::Value(200.0f);
data["isVisible"] = cocos2d::Value(true);

// 读取数据
int id = data["id"].asInt();
std::string name = data["name"].asString();
float x = data["positionX"].asFloat();
bool visible = data["isVisible"].asBool();

// 检查键是否存在
if (data.count("id")) {
    CCLOG("id exists");
}
```

### 8. Vec2（二维向量）

**概念**：Vec2表示二维坐标或向量，用于位置、速度等。

**常用操作**：
```cpp
// 创建向量
cocos2d::Vec2 pos(100, 200);
cocos2d::Vec2 zero = cocos2d::Vec2::ZERO;

// 向量运算
cocos2d::Vec2 v1(10, 20);
cocos2d::Vec2 v2(30, 40);

// 加法
cocos2d::Vec2 sum = v1 + v2;  // (40, 60)

// 减法
cocos2d::Vec2 diff = v2 - v1;  // (20, 20)

// 乘法（标量）
cocos2d::Vec2 scaled = v1 * 2.0f;  // (20, 40)

// 距离
float distance = v1.getDistance(v2);

// 长度
float length = v1.getLength();

// 归一化
cocos2d::Vec2 normalized = v1.getNormalized();
```

### 9. Size（尺寸）

**概念**：Size表示二维尺寸，用于设置节点的大小。

**使用示例**：
```cpp
// 创建尺寸
cocos2d::Size size(100, 150);
cocos2d::Size winSize = cocos2d::Director::getInstance()->getWinSize();

// 设置节点大小
sprite->setContentSize(cocos2d::Size(100, 150));

// 获取节点大小
cocos2d::Size contentSize = sprite->getContentSize();
```

### 10. Color（颜色）

**概念**：Color表示颜色，用于设置节点的颜色。

**使用示例**：
```cpp
// 创建颜色
cocos2d::Color3B color(255, 0, 0);  // 红色
cocos2d::Color4B colorWithAlpha(255, 0, 0, 128);  // 半透明红色

// 设置节点颜色
sprite->setColor(cocos2d::Color3B::RED);

// 设置节点透明度
sprite->setOpacity(128);  // 0-255
```

---

## 项目资源说明

### 1. 字体资源

**位置**：`Resources/fonts/`

**字体文件**：
- `Marker Felt.ttf` - 默认字体
- `arial.ttf` - Arial字体
- `msyh.ttc` - 微软雅黑（支持中文）
- `msyhbd.ttc` - 微软雅黑粗体
- `msyhl.ttc` - 微软雅黑细体

**使用方法**：
```cpp
// 创建标签
auto label = cocos2d::Label::createWithTTF("Hello World", "fonts/msyh.ttc", 24);
label->setPosition(cocos2d::Vec2(100, 100));
this->addChild(label);
```

### 2. 数字图片资源

**位置**：`Resources/res/number/`

**命名规则**：
- `big_red_X.png` - 大号红色数字（X为点数）
- `big_black_X.png` - 大号黑色数字
- `small_red_X.png` - 小号红色数字

**点数对应**：
- `A` - Ace（1）
- `2-10` - 数字2-10
- `J` - Jack（11）
- `Q` - Queen（12）
- `K` - King（13）

**使用方法**：
```cpp
// 创建数字精灵
auto numberSprite = cocos2d::Sprite::create("res/number/big_red_A.png");
numberSprite->setPosition(cocos2d::Vec2(10, 140));
this->addChild(numberSprite);
```

### 3. 花色图片资源

**位置**：`Resources/res/suits/`

**花色图片**：
- `heart.png` - 红心（红色）
- `diamond.png` - 方块（红色）
- `club.png` - 梅花（黑色）
- `spade.png` - 黑桃（黑色）

**使用方法**：
```cpp
// 创建花色精灵
auto suitSprite = cocos2d::Sprite::create("res/suits/heart.png");
suitSprite->setPosition(cocos2d::Vec2(90, 10));
this->addChild(suitSprite);
```

### 4. 卡牌背景资源

**位置**：`Resources/res/card_general.png`

**说明**：卡牌的白色背景图片，用于所有卡牌的底色。

**使用方法**：
```cpp
// 创建卡牌背景
auto background = cocos2d::Sprite::create("res/card_general.png");
background->setContentSize(cocos2d::Size(100, 150));
this->addChild(background);
```

### 5. 关卡配置文件

**位置**：`Resources/level_config.json`

**格式**：JSON格式

**结构**：
```json
{
    "Playfield": [
        {
            "CardFace": 12,
            "CardSuit": 0,
            "Position": {"x": 250, "y": 1000}
        }
    ],
    "Stack": [
        {
            "CardFace": 2,
            "CardSuit": 0,
            "Position": {"x": 0, "y": 0}
        }
    ]
}
```

**字段说明**：
- `Playfield` - 桌面卡牌数组
- `Stack` - 手牌堆数组
- `CardFace` - 卡牌点数（0-12，对应A-K）
- `CardSuit` - 卡牌花色（0-3，对应红心、方块、梅花、黑桃）
- `Position` - 卡牌位置

**加载方法**：
```cpp
// 使用LevelConfigLoader加载配置
configs::LevelConfigLoader* loader = configs::LevelConfigLoader::getInstance();
configs::LevelConfig* levelConfig = loader->loadLevelConfig(levelId);
```

---

## 游戏流程详解

### 1. 应用程序启动流程

```
main() → AppDelegate::applicationDidFinishLaunching()
    ↓
1. 初始化Director
2. 创建游戏窗口
3. 设置帧率
4. 创建GameController
5. 调用GameController::startGame()
```

**代码示例**：
```cpp
bool AppDelegate::applicationDidFinishLaunching() {
    // 初始化Director
    auto director = cocos2d::Director::getInstance();
    auto glview = director->getOpenGLView();
    
    if (!glview) {
        glview = cocos2d::GLViewImpl::create("Card Game");
        director->setOpenGLView(glview);
    }
    
    // 设置帧率
    director->setAnimationInterval(1.0 / 60);
    
    // 创建游戏控制器
    auto controller = new controllers::GameController();
    if (controller->init()) {
        controller->startGame(1);  // 开始第一关
    }
    
    return true;
}
```

### 2. 游戏初始化流程

```
GameController::startGame(levelId)
    ↓
1. 加载关卡配置
2. 生成游戏模型
3. 初始化控制器
4. 初始化游戏视图
5. 设置事件监听器
6. 创建所有卡牌视图
7. 更新UI
8. 播放入场动画
9. 运行场景
```

**代码示例**：
```cpp
void GameController::startGame(int levelId) {
    // 1. 加载关卡配置
    configs::LevelConfigLoader* loader = configs::LevelConfigLoader::getInstance();
    configs::LevelConfig* levelConfig = loader->loadLevelConfig(levelId);
    
    // 2. 生成游戏模型
    generators::GameModelFromLevelGenerator* generator = 
        generators::GameModelFromLevelGenerator::getInstance();
    _gameModel = generator->generateGameModel(*levelConfig);
    
    // 3. 初始化控制器
    _playFieldController->init(_gameModel);
    _stackController->init(_gameModel);
    
    // 4. 初始化游戏视图
    initGameView();
    
    // 5. 设置事件监听器
    setupEventListeners();
    
    // 6. 创建所有卡牌视图
    for (const auto& pair : _gameModel->getAllCards()) {
        CardView* cardView = CardView::createWithModel(pair.second);
        if (cardView) {
            _gameView->addCardView(cardView);
        }
    }
    
    // 7. 更新UI
    updateUI();
    
    // 8. 播放入场动画
    _gameView->playEntryAnimation();
    
    // 9. 运行场景
    cocos2d::Director::getInstance()->runWithScene(_gameView);
}
```

### 3. 卡牌点击流程

```
用户点击卡牌 → CardView捕获触摸事件
    ↓
CardView::onTouchEnded() → 调用点击回调
    ↓
GameController::handleCardClick()
    ↓
判断卡牌类型
    ├─ 手牌 → replaceTopHandCard()
    │   ↓
    │   1. 获取当前顶手牌
    │   2. 设置新的顶手牌
    │   3. 播放替换动画
    │   4. 记录操作到撤销管理器
    │
    └─ 桌面牌 → matchTableCard()
        ↓
        1. 获取顶手牌
        2. 检查是否可以匹配
        3. 如果可以匹配：
           a. 播放匹配动画
           b. 从游戏中移除两张卡牌
           c. 记录操作到撤销管理器
```

**代码示例**：
```cpp
bool GameController::handleCardClick(CardView* cardView) {
    // 获取卡牌ID和模型
    int cardId = cardView->getCardId();
    CardModel* card = _gameModel->getCardById(cardId);
    
    if (!card) {
        return false;
    }
    
    // 判断卡牌类型
    if (card->getType() == CardConfig::CardType::HAND_CARD) {
        // 处理手牌点击
        replaceTopHandCard(cardId);
    } else {
        // 处理桌面牌点击
        matchTableCard(cardId);
    }
    
    return true;
}
```

### 4. 撤销/重做流程

```
用户点击撤销按钮 → GameController::handleUndoButtonClick()
    ↓
GameController::executeUndo()
    ↓
1. 设置撤销/重做标志
2. 从UndoManager获取上一个状态
3. 反序列化游戏状态
4. 更新所有卡牌视图
5. 清除撤销/重做标志
```

**代码示例**：
```cpp
void GameController::executeUndo() {
    // 设置撤销/重做标志
    _isPerformingUndoRedo = true;
    
    // 从UndoManager获取上一个状态
    models::ActionRecord* record = _undoManager->undo();
    
    if (record) {
        // 反序列化游戏状态
        _gameModel->deserialize(record->prevState);
        
        // 更新所有卡牌视图
        _gameView->updateGameView(_gameModel);
        
        // 更新UI
        updateUI();
    }
    
    // 清除撤销/重做标志
    _isPerformingUndoRedo = false;
}
```

---

## 关键设计模式

### 1. MVC模式（Model-View-Controller）

**说明**：将应用程序分为模型、视图和控制器三个部分。

**优点**：
- 代码结构清晰
- 易于维护和扩展
- 模块之间低耦合

**本项目的实现**：
- Model：GameModel、CardModel
- View：GameView、CardView
- Controller：GameController、CardController

### 2. 单例模式（Singleton）

**说明**：确保一个类只有一个实例，并提供全局访问点。

**优点**：
- 节省内存
- 全局访问
- 统一管理

**本项目的实现**：
```cpp
// CommandService单例
class CommandService {
private:
    static CommandService* _instance;
    CommandService();  // 私有构造函数
    
public:
    static CommandService* getInstance() {
        if (!_instance) {
            _instance = new CommandService();
        }
        return _instance;
    }
    
    static void destroyInstance() {
        if (_instance) {
            delete _instance;
            _instance = nullptr;
        }
    }
};
```

### 3. 命令模式（Command）

**说明**：将请求封装为对象，从而可以用不同的请求对客户进行参数化。

**优点**：
- 支持撤销/重做
- 易于扩展新命令
- 解耦请求发送者和接收者

**本项目的实现**：
```cpp
// 命令服务
void CommandService::executeReplaceTopCardCommand(int clickedCardId, GameModel* gameModel) {
    // 保存操作前的状态
    cocos2d::ValueMap prevState = gameModel->serialize();
    
    // 执行操作
    int topCardId = gameModel->getTopHandCardId();
    gameModel->setTopHandCardId(clickedCardId);
    
    // 保存操作后的状态
    cocos2d::ValueMap nextState = gameModel->serialize();
    
    // 记录操作
    recordAction(ActionType::REPLACE_TOP_CARD, clickedCardId, topCardId, 
                 prevState, nextState);
}
```

### 4. 工厂模式（Factory）

**说明**：定义一个创建对象的接口，让子类决定实例化哪个类。

**优点**：
- 解耦对象的创建和使用
- 易于扩展新产品
- 代码复用

**本项目的实现**：
```cpp
// CardModel工厂方法
static CardModel* createFromConfig(const CardConfig::CardData& configData) {
    CardModel* model = new CardModel();
    if (model) {
        model->_id = configData.id;
        model->_suit = configData.suit;
        model->_rank = configData.rank;
        model->_type = configData.type;
        model->_position = configData.position;
        model->_textureName = configData.textureName;
        return model;
    }
    delete model;
    return nullptr;
}
```

### 5. 观察者模式（Observer）

**说明**：定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都得到通知。

**优点**：
- 解耦观察者和被观察者
- 支持广播通信
- 易于扩展

**本项目的实现**：
```cpp
// 回调函数机制
typedef std::function<void(CardView*)> CardClickCallback;

// 设置回调
void setClickCallback(const CardClickCallback& callback) {
    _clickCallback = callback;
}

// 触发回调
void onTouchEnded(cocos2d::Touch* touch, cocos2d::Event* event) {
    if (_clickCallback) {
        _clickCallback(this);  // 通知观察者
    }
}
```

---

## 常用函数说明

### 1. 场景管理

```cpp
// 运行场景（第一次启动）
cocos2d::Director::getInstance()->runWithScene(scene);

// 替换场景
cocos2d::Director::getInstance()->replaceScene(scene);

// 推入场景（场景栈）
cocos2d::Director::getInstance()->pushScene(scene);

// 弹出场景
cocos2d::Director::getInstance()->popScene();

// 获取当前场景
cocos2d::Scene* currentScene = cocos2d::Director::getInstance()->getRunningScene();
```

### 2. 节点管理

```cpp
// 添加子节点
parent->addChild(child);
parent->addChild(child, zOrder);  // 指定Z轴值
parent->addChild(child, zOrder, tag);  // 指定标签

// 移除子节点
parent->removeChild(child);
parent->removeChildByTag(tag);
parent->removeAllChildren();

// 获取子节点
Node* child = parent->getChildByTag(tag);

// 从父节点移除
child->removeFromParent();
```

### 3. 位置和尺寸

```cpp
// 设置位置
node->setPosition(cocos2d::Vec2(x, y));
node->setPositionX(x);
node->setPositionY(y);

// 获取位置
cocos2d::Vec2 pos = node->getPosition();
float x = node->getPositionX();
float y = node->getPositionY();

// 设置尺寸
node->setContentSize(cocos2d::Size(width, height));

// 获取尺寸
cocos2d::Size size = node->getContentSize();

// 获取边界框
cocos2d::Rect rect = node->getBoundingBox();
```

### 4. 动画

```cpp
// 运行动作
node->runAction(action);

// 停止所有动作
node->stopAllActions();

// 停止指定动作
node->stopAction(action);

// 移动到
auto move = cocos2d::MoveTo::create(duration, cocos2d::Vec2(x, y));

// 移动相对距离
auto moveBy = cocos2d::MoveBy::create(duration, cocos2d::Vec2(dx, dy));

// 缩放到
auto scale = cocos2d::ScaleTo::create(duration, sx, sy);

// 旋转到
auto rotate = cocos2d::RotateTo::create(duration, angle);

// 淡出
auto fadeOut = cocos2d::FadeOut::create(duration);

// 淡入
auto fadeIn = cocos2d::FadeIn::create(duration);

// 序列动作
auto sequence = cocos2d::Sequence::create(action1, action2, nullptr);

// 并行动作
auto spawn = cocos2d::Spawn::create(action1, action2, nullptr);

// 重复动作
auto repeat = cocos2d::Repeat::create(action, count);
auto repeatForever = cocos2d::RepeatForever::create(action);

// 延迟
auto delay = cocos2d::DelayTime::create(duration);

// 回调
auto callback = cocos2d::CallFunc::create([](){
    CCLOG("Callback");
});
```

### 5. 事件处理

```cpp
// 添加事件监听器
cocos2d::Director::getInstance()->getEventDispatcher()
    ->addEventListenerWithSceneGraphPriority(listener, node);

// 移除事件监听器
cocos2d::Director::getInstance()->getEventDispatcher()
    ->removeEventListener(listener);

// 暂停事件监听
cocos2d::Director::getInstance()->getEventDispatcher()
    ->pauseEventListenersForTarget(node);

// 恢复事件监听
cocos2d::Director::getInstance()->getEventDispatcher()
    ->resumeEventListenersForTarget(node);
```

### 6. 调度器

```cpp
// 调度器单例
auto scheduler = cocos2d::Director::getInstance()->getScheduler();

// 定时回调
scheduler->schedule([this](float dt){
    CCLOG("Update: %f", dt);
}, this, 1.0f / 60, false, "update");

// 取消调度
scheduler->unschedule("update", this);

// 暂停调度
scheduler->pauseTarget(this);

// 恢复调度
scheduler->resumeTarget(this);
```

### 7. 日志输出

```cpp
// 输出日志
CCLOG("Message");
CCLOG("Value: %d", value);
CCLOG("Position: (%f, %f)", pos.x, pos.y);

// 警告
CCWARN("Warning message");

// 错误
CCERROR("Error message");
```

---

## 总结

这个卡牌游戏项目采用了清晰的MVC架构，代码结构合理，易于理解和扩展。通过学习这个项目，你可以掌握：

1. **Cocos2d-x游戏开发**：场景管理、节点操作、动画系统、事件处理
2. **软件设计模式**：MVC、单例、命令、工厂、观察者模式
3. **游戏架构设计**：模块化设计、分层架构、数据管理
4. **游戏开发流程**：从初始化到游戏运行的完整流程

建议的学习路径：
1. 先理解MVC架构和项目结构
2. 学习Cocos2d-x的核心概念
3. 阅读核心代码（GameController、GameModel、GameView）
4. 运行游戏，观察效果
5. 修改代码，实验不同功能
6. 扩展新功能，提升技能

祝你学习顺利！
