# 卡牌游戏代码架构导读

## 目录
1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [核心概念](#核心概念)
4. [代码导读路线](#代码导读路线)
5. [各层详细说明](#各层详细说明)
6. [阅读代码的建议](#阅读代码的建议)

---

## 项目概述

这是一个基于Cocos2d-x引擎开发的卡牌匹配游戏，玩家需要通过匹配相同花色和点数的卡牌来消除桌面上的牌。

### 主要功能
- 卡牌匹配：点击桌面牌，如果与顶手牌花色和点数相同，则消除两张牌
- 手牌替换：点击手牌，将其设置为新的顶手牌
- 撤销/重做：支持撤销和重做操作
- 关卡系统：支持多个关卡

### 技术栈
- 引擎：Cocos2d-x 3.x
- 语言：C++
- 架构：MVC（Model-View-Controller）

---

## 整体架构

项目采用经典的MVC架构，分为三个主要层次：

```
┌─────────────────────────────────────────────────────────┐
│                    视图层 (View)                      │
│  ┌─────────────┐  ┌─────────────┐                   │
│  │  GameView   │  │  CardView   │                   │
│  └─────────────┘  └─────────────┘                   │
└─────────────────────────────────────────────────────────┘
                        ?
┌─────────────────────────────────────────────────────────┐
│                  控制器层 (Controller)                 │
│  ┌──────────────┐  ┌──────────────┐                 │
│  │GameController│  │CardController│                 │
│  └──────────────┘  └──────────────┘                 │
│  ┌──────────────┐  ┌──────────────┐                 │
│  │PlayField     │  │Stack         │                 │
│  │Controller    │  │Controller    │                 │
│  └──────────────┘  └──────────────┘                 │
└─────────────────────────────────────────────────────────┘
                        ?
┌─────────────────────────────────────────────────────────┐
│                   模型层 (Model)                      │
│  ┌─────────────┐  ┌─────────────┐                   │
│  │ GameModel   │  │ CardModel   │                   │
│  └─────────────┘  └─────────────┘                   │
└─────────────────────────────────────────────────────────┘
                        ?
┌─────────────────────────────────────────────────────────┐
│                   服务层 (Service)                    │
│  ┌──────────────┐  ┌──────────────┐                 │
│  │CardService   │  │CommandService│                 │
│  └──────────────┘  └──────────────┘                 │
└─────────────────────────────────────────────────────────┘
                        ?
┌─────────────────────────────────────────────────────────┐
│                   配置层 (Config)                      │
│  ┌──────────────┐  ┌──────────────┐                 │
│  │CardConfig    │  │LevelConfig   │                 │
│  └──────────────┘  └──────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

---

## 核心概念

### 1. MVC架构
- **Model（模型）**：负责数据存储和业务逻辑
- **View（视图）**：负责显示和用户交互
- **Controller（控制器）**：负责协调模型和视图

### 2. 卡牌类型
- **手牌（HAND_CARD）**：玩家可以点击替换的牌，堆叠在紫色区域
- **桌面牌（TABLE_CARD）**：显示在橙色区域的牌，用于匹配
- **顶手牌**：手牌堆最上面的一张，可以用来匹配桌面牌

### 3. 匹配规则
- 两张牌的点数绝对值相差1

### 4. 撤销/重做
- 使用命令模式实现
- 保存操作前后的游戏状态
- 使用栈结构管理历史记录

---

## 代码导读路线

### 第一步：理解数据模型（Model层）

**阅读顺序**：
1. [CardModel.h](D:\Code\cocos_work\card\Classes\models\CardModel.h) - 卡牌数据模型
2. [CardModel.cpp](d:\Code\cocos_work\card\Classes\models\CardModel.cpp) - 卡牌模型实现
3. [GameModel.h](d:\Code\cocos_work\card\Classes\models\GameModel.h) - 游戏数据模型
4. [GameModel.cpp](d:\Code\cocos_work\card\Classes\models\GameModel.cpp) - 游戏模型实现

**学习目标**：
- 理解卡牌的数据结构（ID、花色、点数、类型、位置等）
- 理解游戏状态的管理（手牌、桌面牌、顶手牌）
- 理解序列化和反序列化的作用

### 第二步：理解视图（View层）

**阅读顺序**：
1. [CardView.h](d:\Code\cocos_work\card\Classes\views\CardView.h) - 卡牌视图
2. [CardView.cpp](d:\Code\cocos_work\card\Classes\views\CardView.cpp) - 卡牌视图实现
3. [GameView.h](d:\Code\cocos_work\card\Classes\views\GameView.h) - 游戏视图

**学习目标**：
- 理解卡牌的显示（背景、数字、花色）
- 理解触摸事件的处理
- 理解动画效果的实现

### 第三步：理解控制器（Controller层）

**阅读顺序**：
1. [GameController.h](d:\Code\cocos_work\card\Classes\controllers\GameController.h) - 游戏主控制器
2. [GameController.cpp](d:\Code\cocos_work\card\Classes\controllers\GameController.cpp) - 游戏控制器实现

**学习目标**：
- 理解控制器的协调作用
- 理解用户交互的处理流程
- 理解游戏逻辑的实现

### 第四步：理解服务（Service层）

**阅读顺序**：
1. [CardService.h](d:\Code\cocos_work\card\Classes\services\CardService.h) - 卡牌服务
2. [CommandService.h](d:\Code\cocos_work\card\Classes\services\CommandService.h) - 命令服务

**学习目标**：
- 理解服务的封装作用
- 理解撤销/重做的实现原理

---

## 各层详细说明

### 1. 模型层（Model）

#### CardModel（卡牌模型）
**文件位置**：
- 头文件：[CardModel.h](d:\Code\cocos_work\card\Classes\models\CardModel.h)
- 实现文件：[CardModel.cpp](d:\Code\cocos_work\card\Classes\models\CardModel.cpp)

**核心功能**：
- 存储卡牌的基本信息（ID、花色、点数、类型）
- 管理卡牌的位置信息（当前位置和原始位置）
- 控制卡牌的显示状态（是否可见、是否为顶手牌）
- 支持序列化和反序列化

**重要方法**：
- `getId()` - 获取卡牌ID
- `getSuit()` / `setSuit()` - 获取/设置花色
- `getRank()` / `setRank()` - 获取/设置点数
- `getType()` / `setType()` - 获取/设置类型
- `serialize()` - 序列化卡牌数据
- `deserialize()` - 反序列化卡牌数据

#### GameModel（游戏模型）
**文件位置**：
- 头文件：[GameModel.h](d:\Code\cocos_work\card\Classes\models\GameModel.h)
- 实现文件：[GameModel.cpp](d:\Code\cocos_work\card\Classes\models\GameModel.cpp)

**核心功能**：
- 存储和管理所有卡牌对象
- 维护手牌和桌面牌的分类
- 跟踪顶手牌的状态
- 支持序列化和反序列化

**重要方法**：
- `addCard()` - 添加卡牌
- `removeCard()` - 移除卡牌
- `getCardById()` - 根据ID获取卡牌
- `getHandCards()` - 获取手牌列表
- `getTableCards()` - 获取桌面牌列表
- `setTopHandCardId()` - 设置顶手牌
- `serialize()` - 序列化游戏状态
- `deserialize()` - 反序列化游戏状态

### 2. 视图层（View）

#### CardView（卡牌视图）
**文件位置**：
- 头文件：[CardView.h](d:\Code\cocos_work\card\Classes\views\CardView.h)
- 实现文件：[CardView.cpp](d:\Code\cocos_work\card\Classes\views\CardView.cpp)

**核心功能**：
- 显示卡牌的外观（背景、数字、花色）
- 处理用户的点击和触摸事件
- 执行卡牌的移动和动画效果
- 管理卡牌的选中状态和顶手牌状态

**重要方法**：
- `createWithModel()` - 使用模型创建卡牌视图
- `initWithModel()` - 使用模型初始化卡牌视图
- `updateWithModel()` - 使用模型更新卡牌视图
- `moveToPosition()` - 移动卡牌到指定位置
- `setSelected()` - 设置卡牌的选中状态
- `setAsTopCard()` - 设置卡牌为顶手牌状态
- `playClickEffect()` - 播放卡牌点击效果

#### GameView（游戏视图）
**文件位置**：
- 头文件：[GameView.h](d:\Code\cocos_work\card\Classes\views\GameView.h)

**核心功能**：
- 显示所有卡牌视图
- 显示游戏UI按钮（撤销、重做、重置）
- 处理卡牌匹配和替换的动画
- 管理卡牌视图的添加、删除和更新

**重要方法**：
- `updateGameView()` - 更新游戏视图
- `addCardView()` - 添加卡牌视图
- `removeCardView()` - 移除卡牌视图
- `playMatchAnimation()` - 播放卡牌匹配动画
- `playReplaceAnimation()` - 播放卡牌替换动画
- `setupInitialLayout()` - 设置初始布局

### 3. 控制器层（Controller）

#### GameController（游戏控制器）
**文件位置**：
- 头文件：[GameController.h](d:\Code\cocos_work\card\Classes\controllers\GameController.h)

**核心功能**：
- 协调模型、视图和其他控制器
- 管理游戏的整体流程（开始、重置、撤销、重做）
- 处理用户交互（卡牌点击、按钮点击）
- 实现游戏的核心逻辑（卡牌匹配、替换）

**重要方法**：
- `init()` - 初始化游戏控制器
- `startGame()` - 开始游戏
- `handleCardClick()` - 处理卡牌点击事件
- `handleUndoButtonClick()` - 处理撤销按钮点击
- `handleRedoButtonClick()` - 处理重做按钮点击
- `isCardsMatchable()` - 检查卡牌是否可匹配
- `updateUI()` - 更新UI

### 4. 服务层（Service）

#### CardService（卡牌服务）
**文件位置**：
- 头文件：[CardService.h](d:\Code\cocos_work\card\Classes\services\CardService.h)

**核心功能**：
- 从配置创建卡牌模型
- 检查卡牌是否可以匹配
- 获取卡牌的纹理名称
- 获取花色和点数的字符串表示

**重要方法**：
- `createCardModelFromConfig()` - 从配置创建卡牌模型
- `isCardsMatchable()` - 检查两张卡牌是否可以匹配
- `getCardTextureName()` - 获取卡牌纹理名称
- `getSuitString()` - 获取花色的字符串表示
- `getRankString()` - 获取点数的字符串表示

#### CommandService（命令服务）
**文件位置**：
- 头文件：[CommandService.h](d:\Code\cocos_work\card\Classes\services\CommandService.h)

**核心功能**：
- 记录游戏中的所有操作
- 保存操作前后的游戏状态
- 执行撤销操作，恢复到上一个状态
- 执行重做操作，恢复到下一个状态
- 管理撤销/重做历史记录

**重要方法**：
- `executeReplaceTopCardCommand()` - 执行替换顶手牌命令
- `executeMatchTableCardCommand()` - 执行匹配桌面卡牌命令
- `undo()` - 执行撤销操作
- `redo()` - 执行重做操作
- `canUndo()` - 检查是否可以撤销
- `canRedo()` - 检查是否可以重做
- `clearHistory()` - 清空历史记录

---

## 阅读代码的建议

### 1. 从简单到复杂
建议按照以下顺序阅读代码：
1. 先读数据模型（CardModel、GameModel）
2. 再读视图（CardView、GameView）
3. 最后读控制器（GameController）
4. 补充阅读服务（CardService、CommandService）

### 2. 理解数据流
理解数据如何在各个层次之间流动：
1. 用户点击卡牌 → 视图层接收事件
2. 视图层通过回调通知控制器
3. 控制器处理逻辑，更新模型
4. 模型变化通知视图更新
5. 视图更新显示

### 3. 关注核心方法
重点关注以下核心方法：
- `GameController::handleCardClick()` - 处理卡牌点击
- `GameController::isCardsMatchable()` - 检查卡牌匹配
- `GameModel::serialize()` / `deserialize()` - 序列化/反序列化
- `CommandService::undo()` / `redo()` - 撤销/重做

### 4. 使用调试工具
建议使用以下方法帮助理解代码：
1. 在关键方法中添加断点
2. 打印关键变量的值
3. 观察程序的执行流程
4. 理解每个方法的作用

### 5. 理解设计模式
项目中使用了多种设计模式：
- **MVC模式**：分离模型、视图和控制器
- **单例模式**：CardService、CommandService
- **命令模式**：撤销/重做功能
- **工厂模式**：CardModel::createFromConfig()
- **观察者模式**：回调函数机制

---

## 总结

这个卡牌游戏项目结构清晰，采用了经典的MVC架构，代码组织良好。通过按照本文档的导读路线阅读代码，您可以：

1. 理解项目的整体架构
2. 掌握各个层次的功能和职责
3. 理解游戏的核心逻辑
4. 学习Cocos2d-x的使用方法
5. 学习C++的设计模式应用

建议您从数据模型开始，逐步深入到视图和控制器，最后理解服务的实现。通过实际运行和调试代码，您将能够更好地理解整个项目的运作机制。

祝您学习愉快！