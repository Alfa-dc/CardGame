# 卡牌游戏项目详细说明书

## 1. 项目概述

这是一个基于Cocos2d-x引擎开发的卡牌匹配游戏。玩家通过点击卡牌来替换顶手牌或匹配桌面牌，当两张牌的点数相差1时即可匹配成功。游戏支持撤销/重做功能，玩家可以通过按钮或快捷键（Z键撤销，Y键重做）来操作。

## 2. 项目结构

```
mycard/
├── Classes/               # 源代码目录
│   ├── configs/           # 配置文件相关
│   │   └── models/        # 配置模型
│   ├── controllers/       # 控制器
│   ├── models/            # 数据模型
│   ├── services/          # 服务层
│   └── views/             # 视图层
├── Resources/             # 资源文件目录
│   ├── fonts/             # 字体文件
│   └── res/               # 卡牌资源
└── proj.win32/            # Windows平台项目文件
```

### 2.1 核心目录说明

| 目录 | 功能说明 |
|------|----------|
| configs | 存放游戏配置相关文件，如卡牌配置、关卡配置等 |
| controllers | 游戏控制器，负责协调模型和视图，处理用户交互 |
| models | 数据模型，存储游戏状态和卡牌数据 |
| services | 服务层，提供各种业务逻辑和工具方法 |
| views | 视图层，负责显示游戏界面和卡牌 |

## 3. 核心类和功能

### 3.1 模型层（Models）

#### 3.1.1 CardModel
- **功能**：存储卡牌的基本数据，包括ID、花色、点数、类型等
- **核心属性**：
  - `_id`：卡牌ID
  - `_suit`：卡牌花色（HEART、DIAMOND、CLUB、SPADE）
  - `_rank`：卡牌点数（1-13）
  - `_type`：卡牌类型（HAND_CARD、TABLE_CARD）
  - `_position`：卡牌位置
  - `_isVisible`：卡牌是否可见
  - `_isTopCard`：是否为顶手牌

#### 3.1.2 GameModel
- **功能**：管理游戏状态和所有卡牌
- **核心方法**：
  - `addCard()`：添加卡牌到游戏中
  - `removeCard()`：从游戏中移除卡牌
  - `getTopHandCard()`：获取顶手牌
  - `setTopHandCardId()`：设置顶手牌ID
  - `serialize()`/`deserialize()`：序列化和反序列化游戏状态，用于撤销/重做

#### 3.1.3 UndoModel
- **功能**：管理撤销/重做历史记录
- **核心方法**：
  - `addActionRecord()`：添加操作记录
  - `undo()`：获取上一个操作记录
  - `redo()`：获取下一个操作记录
  - `canUndo()`/`canRedo()`：检查是否可以撤销/重做

### 3.2 视图层（Views）

#### 3.2.1 CardView
- **功能**：显示卡牌的外观，处理用户点击事件
- **核心方法**：
  - `createWithModel()`：根据卡牌模型创建卡牌视图
  - `updateWithModel()`：根据卡牌模型更新视图
  - `moveToPosition()`：移动卡牌到指定位置
  - `setSelected()`：设置卡牌选中状态
  - `setAsTopCard()`：设置卡牌为顶手牌状态

#### 3.2.2 GameView
- **功能**：显示游戏场景和所有UI元素
- **核心方法**：
  - `addCardView()`：添加卡牌视图
  - `removeCardView()`：移除卡牌视图
  - `updateGameView()`：更新游戏视图
  - `playMatchAnimation()`：播放卡牌匹配动画
  - `playReplaceAnimation()`：播放卡牌替换动画
  - `playUndoAnimation()`：播放撤销动画

### 3.3 控制器层（Controllers）

#### 3.3.1 GameController
- **功能**：协调游戏各组件，管理游戏流程
- **核心方法**：
  - `init()`：初始化游戏控制器
  - `startGame()`：开始游戏
  - `handleCardClick()`：处理卡牌点击事件
  - `handleUndoButtonClick()`：处理撤销按钮点击
  - `handleRedoButtonClick()`：处理重做按钮点击
  - `handleResetButtonClick()`：处理重置按钮点击
  - `isCardsMatchable()`：检查卡牌是否可匹配

### 3.4 服务层（Services）

#### 3.4.1 CardService
- **功能**：提供卡牌相关的业务逻辑和工具方法
- **核心方法**：
  - `isCardsMatchable()`：检查两张卡牌是否可以匹配
  - `getCardTextureName()`：获取卡牌纹理名称

#### 3.4.2 CommandService
- **功能**：管理游戏中的操作命令和撤销/重做功能
- **核心方法**：
  - `executeReplaceTopCardCommand()`：执行替换顶手牌命令
  - `executeMatchTableCardCommand()`：执行匹配桌面卡牌命令
  - `undo()`：执行撤销操作
  - `redo()`：执行重做操作
  - `clearHistory()`：清空操作历史

## 4. 游戏流程

1. **游戏启动**：
   - AppDelegate初始化游戏引擎
   - 创建GameController实例
   - 调用startGame()方法加载关卡

2. **关卡加载**：
   - 加载指定关卡的配置
   - 根据配置生成游戏模型
   - 创建游戏视图
   - 设置事件监听器
   - 创建所有卡牌视图
   - 播放入场动画

3. **游戏进行**：
   - 玩家点击手牌：替换顶手牌
   - 玩家点击桌面牌：检查是否可以匹配
   - 匹配成功：播放匹配动画，移除两张卡牌
   - 匹配失败：输出日志信息

4. **撤销/重做**：
   - 点击撤销按钮或按Z键：恢复上一个操作
   - 点击重做按钮或按Y键：执行下一个操作

5. **游戏重置**：
   - 点击重置按钮：重新开始当前关卡
   - 清空撤销/重做历史

## 5. 卡牌匹配规则

- **匹配条件**：两张牌的点数相差1
- **示例**：3和4可以匹配，5和6可以匹配，A（1）和2可以匹配，K（13）和Q（12）可以匹配
- **不匹配示例**：3和5不可以匹配，A和K不可以匹配

## 6. 如何增加功能

### 6.1 添加新的卡牌类型

1. 在`CardConfig.h`中添加新的卡牌类型枚举
2. 在`CardModel.h`中添加对应的属性和方法
3. 在`CardView.cpp`中添加对应的显示逻辑
4. 在`CardService.cpp`中添加对应的纹理名称获取方法

### 6.2 添加新的游戏模式

1. 创建新的游戏模型类，继承自`GameModel`
2. 创建新的游戏视图类，继承自`GameView`
3. 创建新的游戏控制器类，继承自`GameController`
4. 在`AppDelegate.cpp`中初始化新的游戏控制器

### 6.3 添加新的匹配规则

1. 在`CardService.cpp`中修改`isCardsMatchable()`方法
2. 根据需要添加新的匹配规则逻辑

### 6.4 添加新的动画效果

1. 在`GameView.cpp`中添加新的动画方法
2. 使用Cocos2d-x的Action系统实现动画效果
3. 在相应的事件处理方法中调用新的动画方法

## 7. 如何改变UI界面

### 7.1 修改卡牌外观

1. 替换`Resources/res/`目录下的卡牌资源图片
2. 在`CardConfig.h`中修改卡牌资源路径配置
3. 在`CardView.cpp`中调整卡牌的显示尺寸和位置

### 7.2 修改游戏背景

1. 在`GameView.cpp`的`setupInitialLayout()`方法中添加背景图片
2. 调整背景图片的尺寸和位置

### 7.3 修改按钮样式

1. 在`GameView.cpp`的`initButtons()`方法中修改按钮的样式
2. 可以使用自定义图片或调整按钮的颜色、字体等属性

### 7.4 调整卡牌布局

1. 在`GameView.cpp`中修改卡牌的位置和间距
2. 可以根据屏幕尺寸动态调整布局

### 7.5 添加新的UI元素

1. 在`GameView.h`中添加新的UI元素成员变量
2. 在`GameView.cpp`的`init()`方法中创建新的UI元素
3. 设置UI元素的位置、样式和回调函数

## 8. 代码优化建议

### 8.1 性能优化

1. **使用对象池**：为卡牌对象创建对象池，避免频繁创建和销毁对象
2. **减少内存占用**：及时释放不再使用的资源，避免内存泄漏
3. **优化动画效果**：减少同时播放的动画数量，使用缓存动画

### 8.2 代码结构优化

1. **分离业务逻辑**：将复杂的业务逻辑分离到单独的服务类中
2. **使用设计模式**：合理使用设计模式，如观察者模式、工厂模式等
3. **提高代码复用性**：提取公共代码到工具类中，避免重复代码

### 8.3 扩展性优化

1. **使用配置文件**：将游戏参数、关卡数据等放入配置文件中，便于修改和扩展
2. **抽象接口**：定义抽象接口，便于添加新的功能和模块
3. **模块化设计**：将游戏功能拆分为独立的模块，便于维护和扩展

## 9. 开发环境配置

- **引擎版本**：Cocos2d-x
- **开发语言**：C++
- **开发工具**：Visual Studio
- **目标平台**：Windows

## 10. 运行和调试

1. 打开`proj.win32/mycard.sln`文件
2. 选择Debug或Release模式
3. 点击运行按钮或按F5键开始调试
4. 游戏窗口将显示，玩家可以通过鼠标点击卡牌进行游戏

## 11. 总结

本项目是一个功能完整的卡牌匹配游戏，具有清晰的MVC架构和良好的扩展性。通过阅读本说明书，开发者可以快速了解游戏的核心功能和代码结构，便于进行功能扩展和UI调整。

游戏支持撤销/重做功能，使用了命令模式和序列化技术来实现。卡牌匹配规则简单易懂，玩家可以快速上手。游戏的代码结构清晰，便于维护和扩展，适合作为学习Cocos2d-x和游戏开发的示例项目。